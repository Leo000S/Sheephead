[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sheephead-common"
version = "0.1.0"
description = "Gemeinsame Logik für Sheephead Spaces"
authors = [{name = "LeoDerLoewe"}]
dependencies = []